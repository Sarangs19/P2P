// import { Button, Text, View } from "react-native"


// export const PeerUI  = () => {

//     return(
//     <View>
//       <Text>Discovered Peers:</Text>
//       {peers.map((peerId) => (
//         <Button
//           key={peerId}
//           title={`Connect to ${peerId}`}
//           onPress={() => connectToPeer(peerId)}
//         />
//       ))}
//       {connected && (
//         <Button title="Send Data" onPress={sendData} />
//       )}
//     </View>
//     )
// }